#!/usr/bin/env bash
set -euo pipefail

echo "This script intentionally does not delete the project."
echo "Phase 0 requires non-destructive setup."
echo "Remove the project directory manually if you are certain it is no longer needed."
